import React from 'react';
import { CheckAuth } from '../../screens/Purchase';
import Layout from '../../components/Purchase/Layout';

const CheckAuthPage = () => {
  return <CheckAuth />;
};

CheckAuthPage.Layout = Layout;

export default CheckAuthPage;
