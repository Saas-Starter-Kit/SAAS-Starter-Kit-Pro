import React from 'react';
import { Login } from '../../screens/Auth';
import Layout from '../../components/Auth/Layout';

const Auth = () => {
  return <Login />;
};

Auth.Layout = Layout;

export default Auth;
