import React from 'react';
import { EmailConfirm } from '../../screens/Auth';
import Layout from '../../components/Auth/Layout';

const EmailConfirmPage = () => {
  return <EmailConfirm />;
};

EmailConfirmPage.Layout = Layout;

export default EmailConfirmPage;
