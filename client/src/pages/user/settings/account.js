import React from 'react';
import Layout from '../../../components/User/Layout';
import { AccountSettings } from '../../../screens/User/Settings';

const Account = () => {
  return <AccountSettings />;
};

Account.Layout = Layout;

export default Account;
